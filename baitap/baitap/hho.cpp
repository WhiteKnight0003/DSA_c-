#include<bits/stdc++.h>

typedef struct TTCM{
	int madetai;
	char nhx[100];
	double mgd;
	double diemcn;
};

void nhapds(char tentep[], int *n; TTCM *&ds){
	FILE *fp=fopen("same.txt","r");
	if(f== NULL)
		printf("Khong doc duoc file");
	int n; fscanf(fp,"%dn",&n);
	ds=(TTCM*) malloc(sizeof(TTCM)*(*n));
	for(int i=0;i<*n;i++){
		fgets(ds[i].madetai,10,f);
		fgets(ds[i].nhx,200,f);
		fscanf(ds[i].mgd)
	}
}
int main(){
	

	for(int i=0;i<n;i++)
	return 0;
}